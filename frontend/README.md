AI PR Reviewer - Frontend

Quick start

1. cd frontend
2. npm install
3. npm run dev

The app is a minimal React + Vite scaffold with a modern responsive UI. It posts review requests to `/review` on the same host — run the Python backend alongside it or proxy requests accordingly.
